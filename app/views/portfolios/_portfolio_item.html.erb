<div class="card" data-id="<%= portfolio.id %>">
  <%= image_tag portfolio.thumb_image_url, width: '100%' unless portfolio.thumb_image_url.nil? %>
  <p class="card-text">
    <span><%= link_to portfolio.title, portfolio_show_path(portfolio) %></span>
    <%= portfolio.subtitle %>
  </p>
</div>